#include <bits/stdc++.h>

using namespace std;

int main() {
    int T;
    cin >> T;

    for (int x = 0; x < T; x++) {
        int a, b, c;
        cin >> a >> b >> c;
        cout << ((4 * a) + (2 * b)) << "\n";
    }
}